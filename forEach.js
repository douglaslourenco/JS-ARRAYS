// EXECUTAR UMA DETERMINADA FUNÇÃO PARA CADA ELEMENTO DO MEU ARRAY

const nomes = ['Douglas', 'Deivisson', 'Esther', 'Maria'];

// Retornar cada elemento do meu array nomes
nomes.forEach(function (elemento) {
    console.log(elemento);
})


const numeros = [5, 10, 15, 20, 25];
// Incrementar + 5 para cada elemento do meu array números
numeros.forEach(function (elemento) {
    elemento = elemento + 5;
    console.log(elemento);
})




